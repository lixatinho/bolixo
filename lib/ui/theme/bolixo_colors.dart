import 'package:flutter/material.dart';

class BolixoColors {
  BolixoColors._();

  // Primary Gradient (backgrounds, AppBar)
  static const deepPlum = Color(0xFF1B0A3C);
  static const royalPurple = Color(0xFF2D1B69);
  static const electricViolet = Color(0xFF6C3CE0);

  // Accent Green (buttons, active states, CTAs)
  static const accentGreen = Color(0xFF059669);
  static const accentGreenLight = Color(0xFF34D399);

  // Accent Cyan (links, progress, secondary highlights)
  static const accentCyan = Color(0xFF06B6D4);

  // Surfaces
  static const backgroundPrimary = Color(0xFF0F0A1A);
  static const backgroundSecondary = Color(0xFF1A1028);
  static const surfaceCard = Color(0xFF221639);
  static const surfaceElevated = Color(0xFF2D1B69);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB8A9D4);
  static const textTertiary = Color(0xFFA498BE);
  static const textOnAccent = Color(0xFFFFFFFF);
  static const textLink = Color(0xFFA78BFA);

  // Semantic
  static const success = Color(0xFF34D399);
  static const error = Color(0xFFF87171);
  static const warning = Color(0xFFFBBF24);
  static const easterEgg = Color(0xFFF7C948);

  // Misc
  static const white6 = Color(0x0FFFFFFF);
  static const white8 = Color(0x14FFFFFF);
  static const white10 = Color(0x1AFFFFFF);
  static const white12 = Color(0x1FFFFFFF);
  static const white15 = Color(0x26FFFFFF);
}
