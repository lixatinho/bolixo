import 'package:flutter/widgets.dart';

class Palpites extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Palpites")
    );
  }
}